## Table

A basic example showing:

* how to use the table widget
* how to map a domain type into a `Row` via `From`
* and how to drive row selection from application state via `Table::new`.

Use the arrow keys to move between rows; the selected language details are shown
in the panel below. Press `Ctrl+C` to exit.
